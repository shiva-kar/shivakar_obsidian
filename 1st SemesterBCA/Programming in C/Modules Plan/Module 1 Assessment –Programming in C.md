# **MODULE 1 – PROGRAMMING IN C**
---

## **SECTION A — ONE MARK QUESTIONS (WITH ANSWERS)**

---

### **1. Q: What is C?**

**A:** A general-purpose, structured programming language.

### **2. Q: Who developed C?**

**A:** Dennis Ritchie.

### **3. Q: Where was C developed?**

**A:** AT&T Bell Labs.

### **4. Q: In which year was C developed?**

**A:** 1972.

### **5. Q: C was developed for writing which operating system?**

**A:** UNIX.

### **6. Q: What type of language is C (low-level, mid-level, high-level)?**

**A:** Mid-level language.

### **7. Q: What does printf() do?**

**A:** Prints output to the console.

### **8. Q: What does scanf() do?**

**A:** Reads input from the user.

### **9. Q: What is a variable?**

**A:** A named memory location.

### **10. Q: What is a constant?**

**A:** A fixed value that cannot be changed during execution.

### **11. Q: What are tokens in C?**

**A:** Smallest valid elements of a program.

### **12. Q: Give an example of a keyword.**

**A:** int.

### **13. Q: How many keywords are in C?**

**A:** 32.

### **14. Q: What is an identifier?**

**A:** The name given to variables, functions, etc.

### **15. Q: What is the main() function?**

**A:** The entry point of a C program.

### **16. Q: What is a data type?**

**A:** Defines type of data a variable can store.

### **17. Q: Size of int on most systems?**

**A:** 4 bytes.

### **18. Q: What is %d used for?**

**A:** Printing integers.

### **19. Q: What does \n mean?**

**A:** New line.

### **20. Q: What is the use of semicolon in C?**

**A:** Marks the end of a statement.

### **21. Q: What is a compiler?**

**A:** A program that converts source code to machine code.

### **22. Q: What is the C compiler in Linux?**

**A:** gcc.

### **23. Q: What is the C compiler in Windows (old)?**

**A:** Turbo C.

### **24. Q: What does return 0 indicate?**

**A:** Successful program termination.

### **25. Q: What does // mean in C?**

**A:** Single-line comment.

### **26. Q: Which header file contains printf()?**

**A:** stdio.h.

### **27. Q: Which symbol starts the preprocessor directive?**

**A:** #.

### **28. Q: What is an operator?**

**A:** A symbol that performs an operation.

### **29. Q: What is assignment operator?**

**A:** =

### **30. Q: What is relational operator?**

**A:** Compares two values.

### **31. Q: What is logical AND operator?**

**A:** &&

### **32. Q: What is logical OR operator?**

**A:** ||

### **33. Q: What is logical NOT operator?**

**A:** !

### **34. Q: What is increment operator?**

**A:** ++

### **35. Q: What is decrement operator?**

**A:** --

### **36. Q: What is the size of char?**

**A:** 1 byte.

### **37. Q: What is an array?**

**A:** A collection of similar data types.

### **38. Q: What is a string in C?**

**A:** A character array ending with null ‘\0’.

### **39. Q: What is loop?**

**A:** A structure that repeats a block of code.

### **40. Q: Name looping statements in C.**

**A:** for, while, do-while.

### **41. Q: What is the exit-controlled loop?**

**A:** do-while.

### **42. Q: What is the entry-controlled loop?**

**A:** for, while.

### **43. Q: What is a function?**

**A:** A block of code that performs a task.

### **44. Q: What is recursion?**

**A:** A function calling itself.

### **45. Q: What is an argument?**

**A:** A value passed to a function.

### **46. Q: What is return type?**

**A:** Data type of value returned by a function.

### **47. Q: What does void mean?**

**A:** No value.

### **48. Q: What is a pointer?**

**A:** A variable that stores memory address.

### **49. Q: What is NULL pointer?**

**A:** A pointer with value NULL (0).

### **50. Q: What is malloc()?**

**A:** Allocates memory dynamically.

### **51. Q: What is free()?**

**A:** Frees dynamically allocated memory.

### **52. Q: What is sizeof operator?**

**A:** Returns size of data type.

### **53. Q: What is break used for?**

**A:** Exit from loop or switch.

### **54. Q: What is continue used for?**

**A:** Skip current iteration.

### **55. Q: What is switch-case used for?**

**A:** Multi-way branching.

### **56. Q: What is default in switch?**

**A:** Executes if no case matches.

### **57. Q: What is structure?**

**A:** User-defined data type with multiple data items.

### **58. Q: What is typedef?**

**A:** Creates alias for existing data type.

### **59. Q: What is union?**

**A:** A data type where all members share same memory.

### **60. Q: What is file handling in C?**

**A:** Reading/writing data to files.

### **61. Q: Which function opens a file?**

**A:** fopen().

### **62. Q: Which function closes a file?**

**A:** fclose().

### **63. Q: What is EOF?**

**A:** End Of File marker.

### **64. Q: What does getch() do?**

**A:** Reads a character without echoing.

### **65. Q: What does puts() do?**

**A:** Prints a string with newline.

### **66. Q: What is a header file?**

**A:** A file containing declarations and function prototypes.

### **67. Q: What is `#include` used for?**

**A:** To include a header file in a program.

### **68. Q: What are escape sequences?**

**A:** Special character combinations starting with backslash.

### **69. Q: What does ‘\t’ represent?**

**A:** Horizontal tab.

### **70. Q: What does ‘\0’ represent?**

**A:** Null character.

### **71. Q: What is a comment?**

**A:** Text ignored by compiler.

### **72. Q: Can C have nested comments?**

**A:** No.

### **73. Q: What is the default return type of main() in modern C?**

**A:** int.

### **74. Q: What is the default value of an uninitialized local variable?**

**A:** Garbage value.

### **75. Q: What is the default value of a global variable?**

**A:** 0.

### **76. Q: What is a library function?**

**A:** Predefined function provided by C libraries.

### **77. Q: Give an example of a library function.**

**A:** sqrt().

### **78. Q: What is a user-defined function?**

**A:** A function written by the programmer.

### **79. Q: What is the purpose of return statement?**

**A:** To return value from a function.

### **80. Q: What is call-by-value?**

**A:** Passing values to functions.

### **81. Q: What is call-by-reference?**

**A:** Passing addresses to functions.

### **82. Q: What is a keyword?**

**A:** Reserved word with predefined meaning.

### **83. Q: Can keywords be used as variable names?**

**A:** No.

### **84. Q: What is storage class?**

**A:** Defines scope, lifetime and visibility of variables.

### **85. Q: Name any storage class.**

**A:** auto.

### **86. Q: What is the scope of a static variable?**

**A:** Local to the function but retains value between calls.

### **87. Q: What is extern keyword used for?**

**A:** To declare a global variable used in another file.

### **88. Q: What is auto keyword used for?**

**A:** Default storage class for local variables.

### **89. Q: What is register variable?**

**A:** A variable stored in CPU register for fast access.

### **90. Q: What is typecasting?**

**A:** Explicit conversion from one data type to another.

### **91. Q: Give an example of typecasting.**

**A:** (float)a.

### **92. Q: What is integer division in C?**

**A:** Division where fractional part is discarded.

### **93. Q: What is modulus operator?**

**A:** % — returns remainder.

### **94. Q: What is operator precedence?**

**A:** Rules that decide the order of evaluation.

### **95. Q: What is associativity?**

**A:** Direction in which operators of same precedence are evaluated.

### **96. Q: What is the ternary operator?**

**A:** ?: (conditional operator).

### **97. Q: What is the size of float?**

**A:** 4 bytes.

### **98. Q: What is the size of double?**

**A:** 8 bytes.

### **99. Q: What is the range of char?**

**A:** –128 to +127 (signed).

### **100. Q: What is precedence of ++ compared to + ?**

**A:** ++ has higher precedence.

---

### **101. Q: What is a syntax error?**

**A:** Violation of language grammar rules.

### **102. Q: What is a logical error?**

**A:** Error in program logic.

### **103. Q: What is a runtime error?**

**A:** Error during program execution.

### **104. Q: What is `#define` used for?**

**A:** Creating constants or macros.

### **105. Q: What is a macro?**

**A:** A code fragment replaced by preprocessor before compilation.

### **106. Q: What is tokenization in C?**

**A:** Breaking a program into tokens.

### **107. Q: What is whitespace?**

**A:** Space, newline or tab.

### **108. Q: What is a nested loop?**

**A:** Loop inside another loop.

### **109. Q: Which loop is guaranteed to execute at least once?**

**A:** do-while.

### **110. Q: What is the format specifier for float?**

**A:** %f.

### **111. Q: What is the format specifier for character?**

**A:** %c.

### **112. Q: What is the format specifier for string?**

**A:** %s.

### **113. Q: Can arrays store mixed data types?**

**A:** No.

### **114. Q: What is the index of the first element in an array?**

**A:** 0.

### **115. Q: What does arr[5] mean?**

**A:** 6th element of array.

### **116. Q: What is a multidimensional array?**

**A:** Array of arrays.

### **117. Q: What is a pointer to pointer?**

**A:** double pointer (e.g., int **p).

### **118. Q: What is NULL?**

**A:** Zero-valued pointer constant.

### **119. Q: What is dangling pointer?**

**A:** Pointer pointing to freed memory.

### **120. Q: What is garbage value?**

**A:** Undefined data stored in uninitialized variable.

---

### **121. Q: What is fopen() used for?**

**A:** Opens a file.

### **122. Q: What is fclose() used for?**

**A:** Closes a file.

### **123. Q: What is fgetc()?**

**A:** Reads a character from file.

### **124. Q: What is fputc()?**

**A:** Writes a character to file.

### **125. Q: What is fgets()?**

**A:** Reads a string from file.

### **126. Q: What is fprintf()?**

**A:** Writes formatted output to a file.

### **127. Q: What is fscanf()?**

**A:** Reads formatted data from file.

### **128. Q: What is feof()?**

**A:** Checks end-of-file condition.

### **129. Q: What is binary file?**

**A:** File storing data in binary format.

### **130. Q: What is text file?**

**A:** File storing human-readable characters.

---

### **131. Q: What is recursion depth?**

**A:** The number of times a function calls itself.

### **132. Q: What is base condition in recursion?**

**A:** Condition to stop recursive calls.

### **133. Q: What is infinite recursion?**

**A:** Recursion with no base case.

### **134. Q: What is stack overflow?**

**A:** Memory overflow due to excessive recursion.

### **135. Q: What is a segmentation fault?**

**A:** Illegal memory access error.

---

### **136. Q: What is a function prototype?**

**A:** Declaration of a function before use.

### **137. Q: What is header guard?**

**A:** Prevents multiple inclusion of header file.

### **138. Q: What is preprocessor?**

**A:** Tool that processes code before compilation.

### **139. Q: What is linking?**

**A:** Combining object files to create executable.

### **140. Q: What is compilation?**

**A:** Translating C code to machine code.
Continuing **ONE-MARK QUESTIONS from 141 to 200**, then I will **immediately start the 2-mark questions**.

### **141. Q: What is an object file?**

**A:** File containing machine code generated after compilation (.obj/.o).

### **142. Q: What is an executable file?**

**A:** Final runnable program produced after linking (.exe/a.out).

### **143. Q: What does getchar() do?**

**A:** Reads a single character from input.

### **144. Q: What does putchar() do?**

**A:** Prints a single character.

### **145. Q: What is buffering?**

**A:** Temporary storing of I/O data before processing.

### **146. Q: What is unformatted I/O?**

**A:** I/O operations without format specifiers (e.g., getchar, putchar).

### **147. Q: What is formatted I/O?**

**A:** I/O using format specifiers (printf, scanf).

### **148. Q: What are actual parameters?**

**A:** Values passed to a function during call.

### **149. Q: What are formal parameters?**

**A:** Variables that receive arguments in function definition.

### **150. Q: What is variable scope?**

**A:** Region where the variable is accessible.

### **151. Q: What is global scope?**

**A:** Variable accessible throughout the program.

### **152. Q: What is local scope?**

**A:** Variable accessible only inside a function/block.

### **153. Q: What is block?**

**A:** Code enclosed within { }.

### **154. Q: What is indentation?**

**A:** Formatting code with spaces for readability.

### **155. Q: What are unary operators?**

**A:** Operators working on a single operand.

### **156. Q: What are binary operators?**

**A:** Operators working on two operands.

### **157. Q: What are arithmetic operators?**

**A:** +, -, *, /, %

### **158. Q: What is left-shift operator?**

**A:** <<

### **159. Q: What is right-shift operator?**

**A:** >>

### **160. Q: What are bitwise operators?**

**A:** &, |, ^, <<, >>, ~

### **161. Q: What is bitwise AND?**

**A:** & operator.

### **162. Q: What is bitwise OR?**

**A:** | operator.

### **163. Q: What is bitwise XOR?**

**A:** ^ operator.

### **164. Q: What is bitwise NOT?**

**A:** ~ operator.

### **165. Q: What is short-circuit evaluation?**

**A:** Logical operators stop evaluation once result is determined.

### **166. Q: What is operator overloading in C?**

**A:** Not supported.

### **167. Q: What is character constant?**

**A:** A single character enclosed in single quotes.

### **168. Q: What is string constant?**

**A:** Characters enclosed in double quotes.

### **169. Q: What is array initialization?**

**A:** Assigning initial values to array at declaration.

### **170. Q: What is strlen()?**

**A:** Function that returns length of string.

### **171. Q: What is strcmp()?**

**A:** Compares two strings.

### **172. Q: What is strcpy()?**

**A:** Copies one string into another.

### **173. Q: What is strcat()?**

**A:** Appends one string to another.

### **174. Q: What is structure member access operator?**

**A:** Dot ( . )

### **175. Q: What is pointer-to-structure operator?**

**A:** Arrow ( -> )

### **176. Q: Can arrays be passed to functions?**

**A:** Yes, always passed by reference.

### **177. Q: What is the return type of malloc()?**

**A:** void* pointer.

### **178. Q: What is calloc()?**

**A:** Allocates multiple blocks of memory and initializes them to zero.

### **179. Q: What is realloc()?**

**A:** Changes size of previously allocated memory.

### **180. Q: What is free()?**

**A:** Deallocates dynamically allocated memory.

### **181. Q: What is segmentation?**

**A:** Dividing program memory into segments.

### **182. Q: What is stack memory used for?**

**A:** Function calls, local variables.

### **183. Q: What is heap memory used for?**

**A:** Dynamic memory allocation.

### **184. Q: What is a preprocessor directive?**

**A:** Instruction executed before compilation.

### **185. Q: What is `#if` used for?**

**A:** Conditional compilation.

### **186. Q: What is `#ifndef` used for?**

**A:** Check if symbol is not defined.

### **187. Q: What is `#pragma` used for?**

**A:** Compiler-specific instructions.

### **188. Q: What is an lvalue?**

**A:** A value that has an address (modifiable).

### **189. Q: What is an rvalue?**

**A:** A temporary value that cannot be referenced.

### **190. Q: What is a symbolic constant?**

**A:** Constant defined using `#define`.

### **191. Q: What is recursion tree?**

**A:** Visual representation of recursive calls.

### **192. Q: What is a sentinel value?**

**A:** Special value that marks end of loop (e.g., –1).

### **193. Q: What is lifetime of variable?**

**A:** Duration for which the variable exists in memory.

### **194. Q: What is fgets()?**

**A:** Reads a string including spaces.

### **195. Q: What is getchar()?**

**A:** Reads a single character.

### **196. Q: What is fflush(stdin)?**

**A:** Undefined behavior (not recommended).

### **197. Q: What is implicit type conversion?**

**A:** Automatic data type conversion by compiler.

### **198. Q: What is explicit type conversion?**

**A:** Manual type conversion (typecasting).

### **199. Q: What is argv in main()?**

**A:** Argument vector (array of strings).

### **200. Q: What is argc in main()?**

**A:** Argument count (number of command-line arguments).

---

## **SECTION B — TWO MARK QUESTIONS (WITH ANSWERS)**

_Now starting 2-mark questions as requested._

---

### **1. Q: Explain the difference between variable and constant.**

**A:**

- A **variable** is a memory location whose value can change during program execution.
    
- A **constant** is a fixed value defined using const or `#define` and cannot be modified.
    

---

### **2. Q: What are keywords? Give examples.**

**A:**  
Keywords are reserved words in C with predefined meaning.  
Examples: int, float, return, break, continue.

---

### **3. Q: What are identifiers? State rules for naming them.**

**A:**  
Identifiers are names for variables, functions, arrays, etc.  
Rules:

- Must start with letter/underscore
    
- No spaces
    
- No special characters except _
    
- Cannot be a keyword
    

---

### **4. Q: Differentiate between printf() and scanf().**

**A:**

- **printf():** Displays output on screen.
    
- **scanf():** Reads input from user into variables.
    

---

### **5. Q: What is the difference between = and == in C?**

**A:**

- **= (assignment):** Assigns value.
    
- **== (equality):** Compares two values.
    
Continuing **TWO-MARK QUESTIONS (6 onward)**.  
All unique, expanded, examiner-level, and strictly Module 1 – C Programming.

---

### **6. Q: What are the different data types in C?**

**A:**  
C provides:

- **Basic types:** int, float, char, double
    
- **Derived types:** arrays, pointers, functions
    
- **User-defined types:** struct, union, enum, typedef
    

---

### **7. Q: What is the purpose of escape sequences? Give two examples.**

**A:**  
Escape sequences represent special characters in strings.  
Examples:

- \n → new line
    
- \t → tab
    

---

### **8. Q: What is typecasting? Give example.**

**A:**  
Typecasting converts one data type to another.  
Example:

```c
(float)a;
```

---

### **9. Q: What is operator precedence? Why is it important?**

**A:**  
Operator precedence decides which operator executes first.  
It prevents ambiguity during expression evaluation.

---

### **10. Q: Explain the difference between while and do-while loops.**

**A:**

- **while:** Entry-controlled; may not execute at all.
    
- **do-while:** Exit-controlled; executes at least once.
    

---

### **11. Q: What are formal and actual parameters?**

**A:**

- **Actual parameters:** Values passed during function call.
    
- **Formal parameters:** Variables that receive values in function definition.
    

---

### **12. Q: What is recursion? Give one example use-case.**

**A:**  
Recursion is a function calling itself.  
Use-case: Factorial, Fibonacci series, tree traversal.

---

### **13. Q: What is an array? How are arrays stored in memory?**

**A:**  
Array is a collection of similar data types.  
Elements are stored in **contiguous memory locations**.

---

### **14. Q: What is a string? How is it represented in C?**

**A:**  
String is a character array ending with '\0'.  
Example: `char s[] = "hello";`.

---

### **15. Q: Explain call by value and call by reference.**

**A:**

- **Call by value:** Copies value; changes don’t affect original.
    
- **Call by reference:** Passes address; changes affect original.
    

---

### **16. Q: What is a pointer? Why is it used?**

**A:**  
Pointer stores memory address.  
Used for: Dynamic memory, arrays, pass-by-reference, efficient code.

---

### **17. Q: What is NULL pointer? Give example.**

**A:**  
Pointer pointing to nothing.  
Example: `int *p = NULL;`

---

### **18. Q: What is malloc()?**

**A:**  
Allocates memory at runtime and returns void* pointer.

---

### **19. Q: State two differences between structure and union.**

**A:**

- Structure allocates memory for each member; union shares memory.
    
- Structure members occupy separate locations; union overwrites.
    

---

### **20. Q: What is a function prototype? Why is it used?**

**A:**  
Declaration of a function before use.  
Used for type checking and informing compiler.

---

### **21. Q: What is a header file? Give two examples.**

**A:**  
Contains declarations and prototypes.  
Examples: stdio.h, math.h.

---

### **22. Q: What is the difference between break and continue?**

**A:**

- **break:** Exits loop/switch.
    
- **continue:** Skips current iteration.
    

---

### **23. Q: What are storage classes? Name them.**

**A:**  
Define scope, lifetime and visibility of variables.  
Classes: auto, static, extern, register.

---

### **24. Q: What is static variable?**

**A:**  
Retains value between function calls.

---

### **25. Q: What is the significance of return type in function?**

**A:**  
Specifies type of value function returns.

---

### **26. Q: What is a multidimensional array? Give example.**

**A:**  
Array of arrays.  
Example: `int a[3][3];`

---

### **27. Q: What is pointer arithmetic? Give example.**

**A:**  
Operations performed on pointers (++, --, +, -).  
Example: `p++ moves to next memory location`.

---

### **28. Q: What is a dangling pointer?**

**A:**  
Pointer referencing freed or invalid memory.

---

### **29. Q: Explain the purpose of sizeof operator.**

**A:**  
Returns memory size of data type or variable.

---

### **30. Q: What is meant by initialization of a variable?**

**A:**  
Assigning a value at the time of declaration.

---

### **31. Q: What is difference between global and local variables?**

**A:**

- Local: Declared inside block; limited scope.
    
- Global: Declared outside; accessible everywhere.
    

---

### **32. Q: What is the difference between ++i and i++?**

**A:**

- **++i:** Pre-increment; increments then returns.
    
- **i++:** Post-increment; returns then increments.
    

---

### **33. Q: What is a symbolic constant?**

**A:**  
Constant defined using #define.

---

### **34. Q: What is an lvalue and rvalue?**

**A:**

- lvalue: Has address; modifiable.
    
- rvalue: Temporary value; not modifiable.
    

---

### **35. Q: What is a library function? Give two examples.**

**A:**  
Predefined function.  
Examples: sqrt(), strlen().

---

### **36. Q: What is a compiler error? Give an example.**

**A:**  
Error detected by compiler.  
Example: Missing semicolon.

---

### **37. Q: What is a runtime error? Give an example.**

**A:**  
Occurs during execution.  
Example: Division by zero.

---

### **38. Q: Why is C called a structured language?**

**A:**  
Uses functions, modular programming and control structures.

---

### **39. Q: What is file pointer?**

**A:**  
Pointer of type FILE* used to access files.

---

### **40. Q: What is the difference between text and binary file?**

**A:**

- Text file: Human-readable characters.
    
- Binary file: Raw binary data.
    

---

### **41. Q: What are the modes to open a file?**

**A:**  
"r", "w", "a", "rb", "wb", "ab" etc.

---

### **42. Q: What is recursion base condition?**

**A:**  
Condition that stops recursive calls.

---

### **43. Q: What is meant by stack overflow?**

**A:**  
Occurs when too many function calls fill the stack.

---

### **44. Q: What is enumeration?**

**A:**  
User-defined integer constants using enum.

---

### **45. Q: What is typedef used for?**

**A:**  
Creates alias for data types.

---

### **46. Q: What is the difference between exit() and return?**

**A:**

- exit(): Terminates program immediately.
    
- return: Exits current function only.
    

---

### **47. Q: What is conditional operator?**

**A:**  
?: used for short decision-making.

---

### **48. Q: What is the significance of main() function?**

**A:**  
Execution of program starts from main().

---

### **49. Q: What are bitwise operators used for?**

**A:**  
Operations on bits (masking, shifting, encryption).

---

### **50. Q: What is a pointer to a function?**

**A:**  
Pointer storing address of a function.

---

### **51. Q: What is meant by implicit type conversion?**

**A:**  
Automatic conversion performed by the compiler when different data types are used in an expression.  
Example: int promoted to float.

---

### **52. Q: What is explicit type conversion?**

**A:**  
Manual conversion using typecasting.  
Example:

```c
(float)num;
```

---

### **53. Q: Explain the purpose of the comma operator.**

**A:**  
Evaluates multiple expressions, returns value of the last expression.

---

### **54. Q: What is the significance of the sizeof() operator?**

**A:**  
Determines memory size of variables or data types to ensure portability.

---

### **55. Q: What is an infinite loop? Give one example.**

**A:**  
Loop that never terminates.  
Example:

```c
while(1) { }
```

---

### **56. Q: Explain nested loops with an example.**

**A:**  
A loop inside another loop.  
Example:

```c
for(i=0;i<3;i++)
    for(j=0;j<3;j++);
```

---

### **57. Q: What is an array bound error?**

**A:**  
Accessing an array index outside declared range (undefined behavior).

---

### **58. Q: What is a pointer? Name two pointer operations.**

**A:**  
Pointer stores address.  
Operations: dereference (*), increment (p++).

---

### **59. Q: What is the use of getchar() and putchar()?**

**A:**

- getchar(): Reads one character
    
- putchar(): Prints one character
    

---

### **60. Q: What is a string literal?**

**A:**  
A constant string enclosed in double-quotes stored in read-only memory.

---

### **61. Q: What does the continue statement do in a loop?**

**A:**  
Skips the remaining statements of current iteration and moves to next iteration.

---

### **62. Q: What is the role of break in loops and switch statements?**

**A:**  
Immediately terminates loop or exits switch block.

---

### **63. Q: What is the difference between getch() and getchar()?**

**A:**

- getchar(): Standard C library, buffered input.
    
- getch(): Non-standard, unbuffered input, no Enter key required.
    

---

### **64. Q: What are library header files? Why are they required?**

**A:**  
Files containing declarations of built-in functions.  
Needed so compiler knows function prototypes.

---

### **65. Q: What is a symbolic constant? Give example.**

**A:**  
Constant created using #define.  
Example: `#define PI 3.14`

---

### **66. Q: What is the purpose of fflush()?**

**A:**  
Clears output buffer.  
Note: `fflush(stdin)` is undefined.

---

### **67. Q: What is a control statement? Name two types.**

**A:**  
Controls flow of execution.  
Types: Decision (if), Looping (for).

---

### **68. Q: What are command-line arguments?**

**A:**  
Arguments passed to program through main(int argc, char *argv[]).

---

### **69. Q: What is the purpose of return 0 in main()?**

**A:**  
Indicates successful program termination to operating system.

---

### **70. Q: State two differences between getchar() and scanf().**

**A:**

- getchar() reads only one character.
    
- scanf() reads formatted input.
    

---

### **71. Q: What is a structure? Why is it used?**

**A:**  
User-defined data type grouping different data types.  
Used for complex data representation.

---

### **72. Q: What is meant by initialization vs assignment?**

**A:**

- Initialization: Value given during declaration.
    
- Assignment: Value given after declaration.
    

---

### **73. Q: What is a file pointer?**

**A:**  
Pointer of type FILE* used to manage file operations.

---

### **74. Q: What is fopen()? Give its syntax.**

**A:**  
Opens a file.  
Syntax: `FILE *fp = fopen("file.txt", "r");`

---

### **75. Q: What is binary mode in file handling?**

**A:**  
Opening files in raw binary format (e.g., "rb", "wb").

---

### **76. Q: What is linear search?**

**A:**  
Sequentially checks every element until match found.

---

### **77. Q: What is recursion? Name a situation where it fails.**

**A:**  
Function calling itself.  
Fails when no base case → stack overflow.

---

### **78. Q: What is the difference between ++i and i++?**

**A:**

- ++i: Pre-increment
    
- i++: Post-increment
    

---

### **79. Q: What does the term "modular programming" mean?**

**A:**  
Dividing program into functions/modules for reusability and readability.

---

### **80. Q: What is the purpose of getchar() and gets()?**

**A:**

- getchar(): Reads 1 char
    
- gets(): Reads string including spaces (unsafe)
    

---

### **81. Q: What is meant by function overloading in C?**

**A:**  
Not supported in C.

---

### **82. Q: What is the difference between const and #define?**

**A:**

- const: Typed, stored in memory
    
- #define: Macro, replaced by preprocessor
    

---

### **83. Q: What is the purpose of the ternary operator?**

**A:**  
Short form of if-else for simple decisions.

---

### **84. Q: What is a character array?**

**A:**  
An array that stores characters; used to store strings.

---

### **85. Q: What are bitwise operators used for?**

**A:**  
Operations on bit-level data like masks and shifting.

---

### **86. Q: What is the significance of the main() function?**

**A:**  
Entry point of C program; execution begins here.

---

### **87. Q: What is the difference between fgets() and gets()?**

**A:**

- fgets(): Safe, limits input size
    
- gets(): Unsafe, causes buffer overflow
    

---

### **88. Q: What is a void pointer?**

**A:**  
Generic pointer that can point to any data type.

---

### **89. Q: Why is C considered a middle-level language?**

**A:**  
Supports both low-level operations (pointers) and high-level constructs (functions).

---

### **90. Q: What is meant by static memory allocation?**

**A:**  
Memory allocated at compile-time (e.g., arrays).

---

### **91. Q: What is dynamic memory allocation?**

**A:**  
Memory allocated at runtime using malloc(), calloc().

---

### **92. Q: What is null-terminated string?**

**A:**  
String ending with '\0'.

---

### **93. Q: What is the difference between fopen() and freopen()?**

**A:**

- fopen(): Opens new file
    
- freopen(): Reopens an existing file stream
    

---

### **94. Q: What is meant by function declaration vs definition?**

**A:**

- Declaration: Prototype
    
- Definition: Actual body of function
    

---

### **95. Q: What is pointer decay?**

**A:**  
Array name decays into pointer to first element during function call.

---

### **96. Q: What is the address-of operator?**

**A:**  
& gives memory address of variable.

---

### **97. Q: What is dereference operator?**

**A:**

- accesses value at a pointer address.
    

---

### **98. Q: What is the purpose of the keyword extern?**

**A:**  
Used to access global variable from another file.

---

### **99. Q: What is a shallow copy?**

**A:**  
Copies only values; pointers still refer to same address.

---

### **100. Q: What is a deep copy?**

**A:**  
Copies actual data plus duplicates pointed-to objects.

---

## **SECTION C — FOUR MARK QUESTIONS (WITH ANSWERS)**

---

### **1. Q: Explain the structure of a C program with an example.**

**A:**  
A C program typically contains:

1. **Header files** (e.g., stdio.h)
    
2. **Global declarations**
    
3. **main() function**
    
4. **Variable declarations**
    
5. **Executable statements**  
    Example:
    

```c
#include <stdio.h>
int main() {
    printf("Hello");
    return 0;
}
```

---

### **2. Q: Explain different data types in C with their sizes.**

**A:**  
C provides:

- **char (1 byte)** – characters
    
- **int (2/4 bytes)** – integers
    
- **float (4 bytes)** – single-precision
    
- **double (8 bytes)** – double-precision  
    Derived types: arrays, pointers, structures, unions  
    User-defined types: enum, struct, typedef.
    

---

### **3. Q: What are the different types of operators in C? Explain with examples.**

**A:**  
C provides:

- **Arithmetic operators** (+, -, *, /, %)
    
- **Relational operators** (==, !=, >)
    
- **Logical operators** (&&, ||, !)
    
- **Bitwise operators** (&, |, ^)
    
- **Assignment operators** (=, +=)
    
- **Conditional operator** (?:)  
    Example: `a = b + 5;`
    

---

### **4. Q: Explain the difference between while, do-while, and for loops.**

**A:**

- **while:** Entry-controlled, checks condition first.
    
- **do-while:** Exit-controlled; runs at least once.
    
- **for:** Best for counter-controlled loops.  
    Syntax differences allow use in different scenarios (e.g., input validation → do-while).
    

---

### **5. Q: What is recursion? Write a recursive function to find factorial.**

**A:**  
Recursion is a technique where a function calls itself.  
Requires a base condition to avoid infinite recursion.  
Example:

```c
int fact(int n){
    if(n==0) return 1;
    return n * fact(n-1);
}
```

---

### **6. Q: Explain call-by-value and call-by-reference with examples.**

**A:**

- **Call-by-value:** Copies value; original not affected.
    
- **Call-by-reference:** Passes address; original changes.  
    Example by reference:
    

```c
void fun(int *p){ (*p)++; }
```

---

### **7. Q: Explain different storage classes in C.**

**A:**

- **auto:** Default local variable
    
- **static:** Retains value between calls
    
- **extern:** Refers to global variable in another file
    
- **register:** Stored in CPU register for speed  
    Each defines scope, lifetime, and visibility.
    

---

### **8. Q: What are arrays? Explain 1D and 2D arrays with examples.**

**A:**  
Array is a collection of similar elements stored consecutively.

- **1D:** `int a[5];`
    
- **2D:** `int b[3][3];`  
    Used for lists, tables, matrices, and structured storage.
    

---

### **9. Q: What are strings? Explain string functions with examples.**

**A:**  
String is a null-terminated char array.  
Common functions:

- `strlen(s)` – length
    
- `strcpy(a,b)` – copy
    
- `strcmp(a,b)` – compare
    
- `strcat(a,b)` – concatenate  
    Example: `strcpy(name, "John");`
    

---

### **10. Q: Explain pointers in C and their advantages.**

**A:**  
Pointer stores an address of a variable.  
Advantages:

1. Efficient array handling
    
2. Dynamic memory allocation
    
3. Pass-by-reference
    
4. Building data structures (linked lists, trees)
    

---

### **11. Q: What is dynamic memory allocation? Explain malloc, calloc, realloc, free.**

**A:**

- **malloc:** Allocates uninitialized memory
    
- **calloc:** Allocates zero-initialized blocks
    
- **realloc:** Resize previously allocated block
    
- **free:** Deallocates memory  
    Used when size required at runtime.
    

---

### **12. Q: Explain structure and union with differences.**

**A:**

- **Structure:** Each member has separate memory
    
- **Union:** All members share the same memory  
    Example struct:
    

```c
struct student{ int id; char name[20]; };
```

Use cases: Struct → full records, Union → memory saving (e.g., variant data).

---

### **13. Q: What is file handling? Explain file opening modes.**

**A:**  
File handling allows reading/writing data from external files.  
Modes:

- **"r"** read
    
- **"w"** write
    
- **"a"** append
    
- **"rb"/"wb"** binary modes  
    Files accessed using `FILE *fp`.
    

---

### **14. Q: What is the difference between strcmp(), strcpy(), and strcat()?**

**A:**

- `strcmp(a,b)` – compares strings
    
- `strcpy(a,b)` – copies b into a
    
- `strcat(a,b)` – appends b to a  
    All require `string.h`.
    

---

### **15. Q: Explain pointer arithmetic with an example.**

**A:**  
Pointer arithmetic is adjusting pointer by size of its data type.  
Example:

```c
int *p = a;  
p++;  // moves to next integer (4 bytes)
```

---

### **16. Q: Explain the syntax and working of if-else ladder.**

**A:**  
Used when multiple mutually exclusive conditions exist.  
Example:

```c
if(x>0) 
    printf("Positive");
else if(x<0)
    printf("Negative");
else 
    printf("Zero");
```

---

### **17. Q: Explain function prototype, function call, and function definition.**

**A:**

- **Prototype:** Declares function (return type + parameters)
    
- **Call:** Invokes function
    
- **Definition:** Actual body  
    Ensures type checking and modular program structure.
    

---

### **18. Q: Explain the difference between prefix and postfix increment.**

**A:**

- **++i:** Increments first, then returns
    
- **i++:** Returns old value, then increments  
    Used differently depending on evaluation order of expressions.
    

---

### **19. Q: Explain the concept of nested loops and give one use-case.**

**A:**  
Loop inside another loop.  
Used in matrices, patterns, 2D arrays.  
Example: printing multiplication table or matrix traversal.

---

### **20. Q: Explain the working of switch-case with an example.**

**A:**  
Used for multi-way branching based on integer/character values.  
Example:

```c
switch(choice){
    case 1: printf("Add"); break;
    case 2: printf("Sub"); break;
    default: printf("Invalid");
}
```

---

### **21. Q: What are the different types of errors in C? Explain with examples.**

**A:**  
Errors in C are:

1. **Syntax errors** – violate grammar rules (e.g., missing semicolon).
    
2. **Logical errors** – incorrect logic (wrong output).
    
3. **Runtime errors** – occur during execution (division by zero).
    
4. **Linker errors** – missing function definitions.  
    Each type must be found and corrected for proper execution.
    

---

### **22. Q: Explain the difference between entry-controlled and exit-controlled loops.**

**A:**

- **Entry-controlled:** Condition checked first (while, for).
    
- **Exit-controlled:** Condition checked after execution (do-while).  
    Entry-controlled may run 0 times; exit-controlled always executes at least once.
    

---

### **23. Q: Explain the continue and break statements with examples.**

**A:**

- **break:** Exits the loop immediately.
    
- **continue:** Skips current iteration and moves to next.  
    Example:
    

```c
for(i=1;i<=5;i++)
    if(i==3) continue;
```

---

### **24. Q: Explain nested if-else with an example.**

**A:**  
Nested if-else means placing one if-else inside another.  
Used for multi-level decision making.  
Example:

```c
if(a>0){
    if(a%2==0) printf("Positive Even");
}
```

---

### **25. Q: Explain the use of switch–case and why break is needed.**

**A:**  
switch selects one block among many based on expression value.  
break prevents fall-through into next case.  
If break is removed, multiple cases execute unintentionally.

---

### **26. Q: What are user-defined functions? Explain their advantages.**

**A:**  
Functions written by programmer.  
Advantages:

- Reusability
    
- Modular structure
    
- Easier debugging
    
- Reduces code size
    
- Enhances readability
    

---

### **27. Q: What are arrays? Explain how to access array elements.**

**A:**  
Array stores multiple values of same type in contiguous memory.  
Elements accessed using index:  
`a[0], a[1] … a[n-1]`.  
Used for lists and sequential data.

---

### **28. Q: Explain different string handling functions in C.**

**A:**

- `strlen()` – finds length
    
- `strcpy()` – copies string
    
- `strcat()` – appends
    
- `strcmp()` – compares  
    Each requires `<string.h>` and operates on char arrays.
    

---

### **29. Q: Explain pointer basics with a diagram representation.**

**A:**  
Pointer stores address of variable.  
Example:

```c
int a = 10;
int *p = &a;
```

Diagram:  
a → [10]  
p → holds address of a.  
Pointers enable dynamic memory, arrays, and efficient code.

---

### **30. Q: Explain pointer to pointer with example.**

**A:**  
Pointer to pointer stores address of another pointer.  
Example:

```c
int x = 5;
int *p = &x;
int **q = &p;
```

Useful in dynamic structures and double-dimensional arrays.

---

### **31. Q: Explain dynamic memory allocation using an example.**

**A:**  
Memory allocated at runtime using malloc/calloc.  
Example:

```c
int *p = malloc(5*sizeof(int));
```

Dynamically allocated memory allows flexible array size and prevents waste.

---

### **32. Q: What are structures? Explain their use with example.**

**A:**  
User-defined datatype grouping different data types.  
Example:

```c
struct student { int id; char name[20]; };
```

Used for records like students, employees, inventory items.

---

### **33. Q: What is a union? How does it save memory?**

**A:**  
Union shares memory among members.  
Largest member decides size.  
Only one member holds valid value at a time.  
Useful when data varies.

---

### **34. Q: Explain passing arrays to functions in C.**

**A:**  
Arrays are passed by reference (pointer to first element).  
Example:

```c
void fun(int a[]) { }
```

Changes inside function affect original array.

---

### **35. Q: Explain the concept of recursion with a suitable example.**

**A:**  
Function calling itself.  
Example: factorial, Fibonacci.  
Needs base condition to prevent infinite recursion.

---

### **36. Q: Explain file handling steps in C.**

**A:**

1. Declare FILE pointer
    
2. Open file using fopen
    
3. Read/Write file
    
4. Close using fclose  
    Used for data storage and retrieval.
    

---

### **37. Q: Explain formatted vs unformatted I/O in C.**

**A:**

- **Formatted:** printf, scanf (uses format specifiers)
    
- **Unformatted:** getchar, putchar, gets, puts  
    Formatted I/O controls precision and formatting; unformatted is simpler.
    

---

### **38. Q: What are bitwise operators? Explain with examples.**

**A:**  
Operate on bits.

- AND (&)
    
- OR (|)
    
- XOR (^)
    
- NOT (~)
    
- Shifts (<<, >>)  
    Example: `x << 1` multiplies x by 2.
    

---

### **39. Q: Explain the difference between structure and array.**

**A:**

- Array: collection of same data type
    
- Structure: collection of different data types  
    Array has fixed size; structure size depends on members.
    

---

### **40. Q: Explain the difference between structure and union with memory diagrams.**

**A:**  
Structure: separate memory for each member.  
Union: shared memory.  
Example:  
struct size = sum of all;  
union size = size of largest member.

---

### **41. Q: Explain nested loops with an example pattern.**

**A:**  
Loops inside loops used for grids, matrices.  
Example:

```
1 2 3  
1 2 3  
```

Created using two nested for loops.

---

### **42. Q: Explain the use of arrays in storing and processing lists.**

**A:**  
Arrays store list-like data: marks, prices, IDs.  
Access via indexing and loop.  
Efficient for fixed-size sequential data.

---

### **43. Q: Explain pointer and array relationship.**

**A:**  
Array name acts as pointer to first element.  
`a[i]` is same as `*(a+i)`.  
Pointers provide flexible iteration.

---

### **44. Q: Explain command-line arguments with example.**

**A:**  
Arguments passed from terminal.  
Example:

```c
int main(int argc, char *argv[])
```

Used for filenames, options, input values in programs.

---

### **45. Q: Explain the difference between gets() and fgets().**

**A:**

- gets(): unsafe, no limit
    
- fgets(): safe, requires size, reads spaces  
    Example: `fgets(str, 20, stdin);`
    

---

### **46. Q: Explain the concept of typecasting with example.**

**A:**  
Converting one type into another.  
Example:

```c
float x = (float)10/3;
```

Ensures correct precision and prevents data loss.

---

### **47. Q: Explain any four string functions with their use.**

**A:**

- `strlen()` – count characters
    
- `strcpy()` – copy
    
- `strcat()` – append
    
- `strcmp()` – compare  
    Used for text processing.
    

---

### **48. Q: Explain the working of a simple calculator using switch-case.**

**A:**  
Switch selects operation based on choice.  
Example operations: + - * /  
Break required to avoid fall-through.  
Used for menu-driven programs.

---

### **49. Q: What is meant by algorithm and flowchart? Explain with an example.**

**A:**

- Algorithm: step-by-step instructions
    
- Flowchart: graphical representation  
    Example: Algorithm + flowchart for adding two numbers.
    

---

### **50. Q: Explain the difference between local, global, and static variables.**

**A:**

- Local: inside function/block
    
- Global: outside all functions
    
- Static: retains value between function calls  
    Each has different scope and lifetime.
    

---

### **51. Q: Explain the difference between actual and formal parameters with an example.**

**A:**

- **Actual parameters:** Values passed during function call
    
- **Formal parameters:** Variables that receive the values in function definition  
    Example:
    

```c
fun(10);        // actual parameter
void fun(int x) // formal parameter
```

Actual → Passed; Formal → Received.

---

### **52. Q: What are the different types of user-defined functions in C?**

**A:**

1. With arguments, with return value
    
2. With arguments, without return value
    
3. Without arguments, with return value
    
4. Without arguments, without return value  
    Used based on program requirements.
    

---

### **53. Q: Explain the scope and lifetime of variables in C.**

**A:**

- **Local variable:** Scope = block; lifetime = during function execution
    
- **Global variable:** Scope = entire program; lifetime = whole execution
    
- **Static variable:** Scope = local; lifetime = full program  
    Scope decides visibility; lifetime decides existence duration.
    

---

### **54. Q: Explain the difference between for and while loops.**

**A:**

- **for:** Used for counter-controlled iteration, compact syntax
    
- **while:** Used when number of iterations unknown  
    for includes init, condition, and increment; while checks condition repeatedly.
    

---

### **55. Q: What is a multi-dimensional array? Give example use.**

**A:**  
Array of arrays.  
Example:

```c
int a[3][3];
```

Used for matrices, grids, tables, 2D game boards, and multi-level data.

---

### **56. Q: Explain strstr(), strcpy(), and strlen() functions.**

**A:**

- **strstr(a,b):** Finds substring b in a
    
- **strcpy(a,b):** Copies string b to a
    
- **strlen(a):** Returns length  
    All require `<string.h>` and operate on char arrays.
    

---

### **57. Q: What is the difference between ++i and i++ when used in expressions?**

**A:**

- **++i:** Increments first, uses incremented value
    
- **i++:** Uses current value, increments after  
    Affects evaluation order in expressions like `a = i++ + 5`.
    

---

### **58. Q: Explain pointer and pointer arithmetic with example.**

**A:**  
Pointer stores address.  
Pointer arithmetic increments by size of datatype.  
Example:

```c
int *p = a;
p++;   // moves to next int (4 bytes)
```

---

### **59. Q: Explain array–pointer equivalence with examples.**

**A:**  
Array name acts as pointer to first element.  
`a[i]` equals `*(a+i)`  
Example:

```c
*(a+2) = a[2];
```

---

### **60. Q: What is a function pointer? Write an example.**

**A:**  
Pointer storing address of function.  
Example:

```c
void (*fp)(int) = &fun;
fp(5);
```

Useful for callbacks and dynamic dispatch.

---

### **61. Q: What is the purpose of typedef? Give an example.**

**A:**  
Provides alias for datatypes.  
Example:

```c
typedef int INTEGER;
INTEGER x = 5;
```

Improves readability.

---

### **62. Q: Explain memory allocation of structure vs union.**

**A:**

- Structure: memory = sum of all members
    
- Union: memory = size of largest member  
    Structure stores all data; union stores one at a time.
    

---

### **63. Q: What are bitwise shift operators? Explain with examples.**

**A:**  
Shift bits left or right.

```c
x << 1  // multiply x by 2
x >> 1  // divide x by 2
```

Used in compression and encryption.

---

### **64. Q: Explain the differences between scanf() and fgets().**

**A:**

- scanf(): stops at space, used for formatted input
    
- fgets(): reads full line including spaces  
    fgets safer and prevents buffer overflow.
    

---

### **65. Q: Explain pass-by-reference using pointers with example.**

**A:**  
Passing address allows modification of original variable.  
Example:

```c
void fun(int *p){ (*p)++; }
fun(&x);
```

x is modified because pointer accesses actual memory.

---

### **66. Q: What are escape sequences? Explain four of them.**

**A:**  
Special characters starting with backslash.  
Examples:

- `\n` newline
    
- `\t` tab
    
- `\\` backslash
    
- `\"` double quote
    

---

### **67. Q: Explain the switch-case fall-through behavior.**

**A:**  
If break is missing, execution continues into next case.  
Example:

```c
case 1:
case 2:
```

Both execute if case 1 matched.

---

### **68. Q: What is the difference between linear search and binary search?**

**A:**

- Linear: sequential, O(n), unsorted allowed
    
- Binary: divide-and-conquer, O(logn), sorted required  
    Binary faster for large sorted arrays.
    

---

### **69. Q: Explain recursion tree and base case.**

**A:**  
Recursion tree shows repeated function calls.  
Base case stops recursion.  
Example: factorial has base case n=0.

---

### **70. Q: Explain how strings are stored in memory.**

**A:**  
Stored as arrays of characters ending with `'\0'`.  
Example:  
`"HELLO"` → H E L L O \0  
Null terminator identifies end.

---

### **71. Q: Explain array initialization methods with examples.**

**A:**

- Full: `int a[3]={1,2,3};`
    
- Partial: `int a[5]={1,2};` (rest 0)
    
- Implicit size: `int a[]={1,2,3};`
    

---

### **72. Q: Explain formatted output specifiers with examples.**

**A:**

- `%d` integer
    
- `%f` float
    
- `%c` char
    
- `%s` string  
    Used with printf/scanf for typing conversion.
    

---

### **73. Q: Explain the difference between local static and global variables.**

**A:**

- Local static: scope = function, lifetime = entire program
    
- Global: scope = whole file  
    Static keeps value between calls.
    

---

### **74. Q: Explain how functions return multiple values using pointers.**

**A:**  
By passing addresses of variables:

```c
void calc(int a,int b,int *s,int *p){
    *s=a+b; *p=a*b;
}
```

Function modifies external values.

---

### **75. Q: Explain file reading using fscanf() with example.**

**A:**  
Reads formatted data from file.

```c
FILE *fp=fopen("data.txt","r");
fscanf(fp,"%d",&n);
```

Used for structured data reading.

---

### **76. Q: Explain the difference between getch() and getchar().**

**A:**

- getch(): non-standard, no Enter key needed
    
- getchar(): standard, buffered  
    getch used for pause or hidden input.
    

---

### **77. Q: What is segmentation fault? Why does it occur?**

**A:**  
Illegal memory access error.  
Causes: accessing freed memory, invalid pointer dereference, array bounds error.

---

### **78. Q: Explain logical operators with examples.**

**A:**

- AND (&&)
    
- OR (||)
    
- NOT (!)  
    Example: `(a > 0 && b > 0)`  
    Used in conditional statements.
    

---

### **79. Q: Explain different file modes with examples.**

**A:**

- “r” – read
    
- “w” – write
    
- “a” – append
    
- “rb” – read binary  
    Example: `fopen("x.bin","rb");`
    

---

### **80. Q: Explain enumerated data type with example.**

**A:**  
User-defined set of integer constants.

```c
enum day {Mon, Tue, Wed};
```

Mon=0, Tue=1, Wed=2.

---

### **81. Q: Explain array traversal using loops.**

**A:**  
Using for-loop to visit each element:

```c
for(i=0;i<n;i++)
    printf("%d",a[i]);
```

Used to process lists.

---

### **82. Q: Explain structure array with example.**

**A:**  
Array of structures.

```c
struct st{sint id;}s[5];
```

Allows storing multiple records.

---

### **83. Q: What is a macro? Explain with example.**

**A:**  
Preprocessor substitution.

```c
#define PI 3.14
```

Replaces PI with 3.14 at compile-time.

---

### **84. Q: Explain pointer initialization and uninitialized pointer problems.**

**A:**  
Pointer must store valid address.  
Uninitialized pointer → garbage address → segmentation fault.

---

### **85. Q: Explain the difference between text and binary file reading.**

**A:**

- Text: human-readable chars
    
- Binary: exact memory bytes  
    Binary used for images, structures.
    

---

### **86. Q: Explain the purpose of fflush() and why fflush(stdin) is wrong.**

**A:**  
fflush clears output buffer.  
`fflush(stdin)` causes undefined behavior in C.

---

### **87. Q: Explain escape sequences and their need.**

**A:**  
Used to represent special characters.  
Examples: '\n', '\t'.  
They allow formatting output.

---

### **88. Q: Explain the difference between pointer and array of pointers.**

**A:**

- Pointer: single address
    
- Array of pointers: multiple addresses  
    Used for strings list, dynamic tables.
    

---

### **89. Q: Explain how to search an element in an array.**

**A:**  
Linear search: check each index.  
Binary search: divided search (sorted array).  
Return index or -1 if not found.

---

### **90. Q: Explain the difference between strcpy() and memcpy().**

**A:**

- strcpy(): string copy until '\0'
    
- memcpy(): byte copy of fixed length  
    memcpy can copy any data.
    

---

### **91. Q: Explain static function.**

**A:**  
Function with file-scope, not accessible outside the file.  
Used to restrict access.

---

### **92. Q: Explain do-while loop with an example.**

**A:**  
Executes once before condition check.

```c
do { printf("Hi"); } while(x<5);
```

---

### **93. Q: Explain passing strings to functions.**

**A:**  
String passed as pointer to character array.  
Example:

```c
void show(char msg[]) { }
```

Function receives pointer.

---

### **94. Q: Explain the concept of buffer overflow.**

**A:**  
Writing beyond memory boundary of array.  
Occurs with gets, unsafe strcpy.  
Causes crashes/security issues.

---

### **95. Q: Explain structure padding.**

**A:**  
Compiler adds bytes for alignment.  
Example:

```c
struct { char c; int i; };
```

Size > expected due to alignment rules.

---

### **96. Q: Explain unary, binary, and ternary operators.**

**A:**

- Unary: one operand (++i)
    
- Binary: two operands (a+b)
    
- Ternary: three operands (?:)
    

---

### **97. Q: Explain how to return an array from a function.**

**A:**  
Return pointer to first element.  
Array must be static or dynamically allocated.

---

### **98. Q: What is a null character and why is it important?**

**A:**  
`'\0'` marks end of string.  
All C string functions depend on it.

---

### **99. Q: Explain the meaning of lvalue and rvalue with examples.**

**A:**

- lvalue: has address → x
    
- rvalue: temporary → x+5  
    lvalue appears on left side of assignment.
    

---

### **100. Q: Explain nested structures with example.**

**A:**  
Structure inside another structure.  
Example:

```c
struct date {int d,m,y;};
struct student {char name[20]; struct date dob;};
```

---
## **SECTION D — LONG QUESTIONS (8–10 MARKS)**

---

### **1. Q: Explain the structure of a C program in detail. Describe each section with examples.**

**A:**  
A C program is divided into several logical sections that determine how the compiler interprets and executes the code. The main parts are:

1. **Documentation Section:**  
    Contains comments describing program purpose, author, date, and logic. Helps maintain readability.  
    Example:
    
    ```c
    /* Program to add two numbers */
    ```
    
2. **Link Section (Header Files):**  
    Includes standard libraries required for functions.  
    Example:
    
    ```c
    #include <stdio.h>
    ```
    
3. **Definition Section:**  
    Contains constants and macro definitions.  
    Example:
    
    ```c
    #define PI 3.14
    ```
    
4. **Global Declaration Section:**  
    Declares global variables and function prototypes accessible throughout the program.  
    Example:
    
    ```c
    int x;
    void fun();
    ```
    
5. **main() Function:**  
    Every C program must have main(). Contains declarations and executable statements.  
    Example:
    
    ```c
    int main() {
        int a, b;
        printf("Hello");
        return 0;
    }
    ```
    
6. **Subprograms (User-defined functions):**  
    Additional functions defined outside main() to support modular programming.
    

A C program resumes execution from main(), but structure ensures organization, readability, and reusability.

---

### **2. Q: Explain data types in C with their classification, size, range, and examples.**

**A:**  
Data types specify the type and size of data stored in variables. C supports three major categories:

1. **Primary/Built-in Types:**
    
    - **int:** stores integers; usually 4 bytes; range −2,147,483,648 to +2,147,483,647
        
    - **char:** 1 byte; stores characters; range −128 to 127
        
    - **float:** 4 bytes; single precision; ~6 digits
        
    - **double:** 8 bytes; double precision  
        Example:
        
    
    ```c
    int age = 20;
    ```
    
2. **Derived Data Types:**
    
    - **Array:** collection of same type (`int a[10];`)
        
    - **Pointer:** stores address (`int *p;`)
        
    - **Function:** returns data type (`int sum();`)
        
    - **Structure:** group of different data types
        
    - **Union:** share same memory  
        These help build complex data structures.
        
3. **User-defined Types:**
    
    - **struct**, **union**, **enum**, **typedef**  
        Example:
        
    
    ```c
    typedef int number;
    ```
    

Data types ensure proper memory allocation and avoid misuse of operations. They are essential in controlling precision, storage, and program behavior.

---

### **3. Q: Explain operators in C along with their precedence and associativity. Provide examples.**

**A:**  
Operators perform operations on operands. Categories:

1. **Arithmetic Operators:**  
    `+ - * / %`  
    Used for mathematical tasks.
    
2. **Relational Operators:**  
    `< <= > >= == !=`  
    Used in comparisons.
    
3. **Logical Operators:**  
    `&& || !`  
    Combine multiple conditions.
    
4. **Assignment Operators:**  
    `= += -= *= /= %=`
    
5. **Unary Operators:**  
    `++ -- sizeof & *`  
    Used for increment, dereference, address-of.
    
6. **Bitwise Operators:**  
    `& | ^ ~ << >>`  
    Used for operations on bits.
    
7. **Conditional Operator:**  
    `?:`  
    Ternary operator for short decision making.
    
8. **Precedence & Associativity:**  
    Determines evaluation order.  
    Example:  
    `*, /` have higher precedence than `+ -`.  
    Associativity for `+ - * /` is left-to-right  
    Example:
    
    ```c
    int a = 10 + 20 * 3; // a = 70
    ```
    

Understanding operator precedence prevents unintended results.

---

### **4. Q: Compare all looping constructs in C: for, while, and do-while. Discuss use-cases with examples.**

**A:**  
Loops repeat a block of code until a condition fails.

1. **for Loop:**  
    Best for counter-based iterations.
    
    ```c
    for(i=0; i<5; i++)
        printf("%d ", i);
    ```
    
    Combines initialization, condition, and increment in one line.
    
2. **while Loop:**  
    Condition checked first; may execute zero times.
    
    ```c
    while(n > 0) {
        printf("%d", n);
        n--;
    }
    ```
    
    Used when number of iterations is unknown beforehand.
    
3. **do-while Loop:**  
    Executes once before checking condition.
    
    ```c
    do {
        scanf("%d",&n);
    } while(n < 0);
    ```
    
    Useful in menu-driven programs.
    

Comparison:

- Use **for** when count known.
    
- Use **while** for event-controlled loops.
    
- Use **do-while** when body must run at least once.
    

---

### **5. Q: Explain functions in C, their types, benefits, and how arguments are passed. Include examples.**

**A:**  
Functions modularize code by breaking tasks into smaller parts.

1. **Built-in Functions:**  
    Provided by libraries (printf, scanf, sqrt).
    
2. **User-defined Functions:**  
    Written by programmer.  
    Types:
    
    - With arg with return
        
    - With arg without return
        
    - No arg with return
        
    - No arg no return
        
3. **Function Prototype:**  
    Declares return type and parameters.  
    Example: `int sum(int,int);`
    
4. **Function Definition:**  
    Contains logic.  
    Example:
    
    ```c
    int sum(int a,int b){
        return a+b;
    }
    ```
    
5. **Call-by-Value:**  
    Copies value; original unchanged.
    
6. **Call-by-Reference:**  
    Passes address; original changes.  
    Example:
    
    ```c
    void update(int *x){ (*x)++; }
    ```
    

Benefits:

- Reusability
    
- Readability
    
- Reduced complexity
    
- Faster debugging
    

---

### **6. Q: Explain arrays in detail. Discuss declaration, memory storage, initialization, and traversal.**

**A:**  
Arrays store multiple values of same data type in continuous memory.

1. **Declaration:**
    
    ```c
    int a[10];
    ```
    
2. **Memory Allocation:**  
    If each int is 4 bytes, `a[10]` takes 40 bytes stored sequentially.
    
3. **Initialization:**
    
    ```c
    int a[5]={1,2,3,4,5};
    int b[]={10,20,30};
    ```
    
4. **Traversal:**  
    Done through loops:
    
    ```c
    for(i=0;i<5;i++)
        printf("%d",a[i]);
    ```
    
5. **Advantages:**
    
    - Fast indexing
        
    - Easy traversal
        
    - Useful for lists, tables, matrices
        
6. **Limitations:**
    
    - Fixed size
        
    - Cannot store mixed data types
        

Arrays are essential for storing sequential and structured data.

---

### **7. Q: Explain strings in C, how they are stored, and describe important string functions.**

**A:**  
String is an array of characters ending with `'\0'`.  
Example:

```c
char name[] = "John";
```

Memory stores characters + null terminator.

**Important String Functions** (from `<string.h>`):

1. `strlen(s)` → length
    
2. `strcpy(a,b)` → copy
    
3. `strcat(a,b)` → concatenate
    
4. `strcmp(a,b)` → compare
    
5. `strupr(s)` → uppercase (non-standard)
    
6. `strlwr(s)` → lowercase (non-standard)
    

Strings allow handling text efficiently.

---

### **8. Q: Explain pointers in C in detail: declaration, dereferencing, pointer arithmetic, and applications.**

**A:**  
Pointer is a variable storing memory address.

1. **Declaration:**
    
    ```c
    int *p;
    ```
    
2. **Initialization:**
    
    ```c
    int x = 10;
    p = &x;
    ```
    
3. **Dereference:**  
    Access value at address.
    
    ```c
    printf("%d", *p);
    ```
    
4. **Pointer Arithmetic:**  
    Depends on data type size.
    
    ```c
    p++;   // moves 4 bytes for int
    ```
    
5. **Applications:**
    
    - Dynamic memory
        
    - Arrays and strings
        
    - Call-by-reference
        
    - Data structures (linked list, tree)
        
    - Function pointers
        

Pointers give C its low-level power.

---

### **9. Q: Illustrate dynamic memory allocation in C. Explain malloc, calloc, realloc, and free with examples.**

**A:**  
Dynamic memory is allocated at runtime.

1. **malloc():**  
    Allocates uninitialized memory.
    
    ```c
    int *p = malloc(5*sizeof(int));
    ```
    
2. **calloc():**  
    Allocates zero-initialized blocks.
    
    ```c
    int *p = calloc(5, sizeof(int));
    ```
    
3. **realloc():**  
    Resizes memory block.
    
    ```c
    p = realloc(p, 10*sizeof(int));
    ```
    
4. **free():**  
    Releases allocated memory.
    
    ```c
    free(p);
    ```
    

Dynamic memory allows flexible data structures and prevents waste.

---

### **10. Q: Explain structures and unions in detail. Compare their memory usage and applications.**

**A:**  
**Structure:**  
User-defined collection of mixed data types.  
Example:

```c
struct student { int id; char name[20]; };
```

Memory allocated separately for each member.

**Union:**  
Shares same memory block among members.  
Example:

```c
union test { int x; float y; };
```

Memory = size of largest member.

**Differences:**

- Structure stores all values; union stores one at a time.
    
- Structure uses more memory; union saves memory.
    

**Applications:**

- Structures: records, objects
    
- Unions: embedded systems, variant data fields
    

---

### **11. Q: Explain file handling in C. Describe file operations, modes, functions, and provide examples.**

**A:**  
File handling allows permanent storage of data outside program memory. C provides the `<stdio.h>` library for file operations.

**1. File Pointer:**  
All file operations use:

```c
FILE *fp;
```

**2. Opening a File:**  
Using `fopen()` with specific modes:

- `"r"` – read
    
- `"w"` – write (overwrites)
    
- `"a"` – append
    
- `"rb"` – read binary
    
- `"wb"` – write binary
    

Example:

```c
fp = fopen("data.txt", "r");
```

**3. Reading/Writing:**

- `fprintf()` / `fscanf()` – formatted I/O
    
- `fgetc()` / `fputc()` – single character
    
- `fgets()` / `fputs()` – strings  
    Example:
    

```c
fprintf(fp, "Hello");
```

**4. Closing File:**

```c
fclose(fp);
```

**5. Error Handling:**  
Check file pointer for NULL:

```c
if(fp == NULL) printf("Cannot open file");
```

File handling ensures data persistence, serialization, and structured storage.

---

### **12. Q: Describe recursion in C. Explain its working, advantages, disadvantages, and examples.**

**A:**  
Recursion is a process where a function calls itself. Essential components:

**1. Base Case:**  
Stops further calls.  
Example:

```c
if(n==0) return 1;
```

**2. Recursive Case:**  
Function calling itself.  
Example:

```c
return n * fact(n-1);
```

**Advantages:**

- Simple code for complex problems
    
- Useful in tree and graph traversal
    
- Natural solution for factorial, Fibonacci, quicksort
    

**Disadvantages:**

- Slow due to overhead
    
- Uses more memory
    
- Can cause stack overflow without base case
    

**Example:**

```c
int fact(int n){
    if(n==0) return 1;
    return n * fact(n-1);
}
```

Recursion should be used when iterative solution becomes too complex.

---

### **13. Q: Explain arrays, multi-dimensional arrays, their memory representation, and examples of usage.**

**A:**  
Array: collection of same type of data stored contiguously.

**1. 1-D Arrays:**

```c
int a[5]={1,2,3,4,5};
```

**2. Memory Representation:**  
Stored in contiguous memory.  
If `a[0]` at 1000 (int=4 bytes),  
`a[1]` at 1004, `a[2]` at 1008 …

**3. 2-D Arrays:**

```c
int b[2][3] = { {1,2,3}, {4,5,6} };
```

**4. Memory Layout (Row-major):**  
Stored row by row.

**5. Traversal:**

```c
for(i=0;i<2;i++)
  for(j=0;j<3;j++)
    printf("%d", b[i][j]);
```

**6. Applications:**

- Matrices
    
- Tables
    
- Images (pixel grids)
    
- Dynamic programming
    

Arrays are essential for storing and manipulating structured data.

---

### **14. Q: Explain string handling in C. Describe storage, important functions, and examples.**

**A:**  
Strings are arrays of characters ending with `'\0'`.

**1. Declaration:**

```c
char s[10] = "Hello";
```

**2. Storage:**  
Characters stored in contiguous memory with null terminator.

**3. Important Functions:**  
From `<string.h>`:

- `strlen(s)` – length
    
- `strcpy(a, b)` – copy
    
- `strcat(a, b)` – append
    
- `strcmp(a, b)` – compare
    
- `strchr(s, c)` – find character
    
- `strstr(s1, s2)` – find substring
    

**4. Example:**

```c
printf("%d", strlen("Hello")); // 5
```

**5. Applications:**

- Password validation
    
- Names and text storage
    
- File path processing
    
- Tokenization
    

Strings allow efficient text manipulation in C.

---

### **15. Q: Explain pointers in detail. Discuss pointer arithmetic, pointers to arrays, pointers to functions, and their importance.**

**A:**  
Pointer is a variable holding memory address.

**1. Declaration:**

```c
int *p;
```

**2. Dereferencing:**  
Access value at address:

```c
*p = 10;
```

**3. Pointer Arithmetic:**  
Moves based on data type size.  
If `p` is int pointer, `p++` moves 4 bytes.

**4. Pointer to Array:**

```c
int a[5];
int *p = a;
```

`*(p+2)` equals `a[2]`.

**5. Pointer to Function:**  
Stores function address:

```c
int (*fp)(int,int) = sum;
```

**6. Applications:**

- Efficient arrays
    
- Dynamic memory
    
- Linked lists
    
- Call-by-reference
    
- Callback systems
    

Pointers give C flexibility, power, and low-level memory access.

---

### **16. Q: Explain structures in C. Discuss declaration, initialization, accessing members, and array of structures.**

**A:**  
Structure groups different data types under one name.

**1. Declaration:**

```c
struct student {
    int id;
    char name[20];
};
```

**2. Initialization:**

```c
struct student s = {101, "John"};
```

**3. Accessing Members:**

```c
s.id = 200;
```

**4. Structure Arrays:**

```c
struct student s[3];
```

Allows storing multiple records.

**5. Nested Structures:**

```c
struct date {int d,m,y;};
struct student {char name[20]; struct date dob;};
```

**6. Applications:**

- Databases
    
- Record systems
    
- Complex objects (employee, product)
    

Structures form the base of data organization.

---

### **17. Q: Explain memory allocation differences between static and dynamic memory in C. Provide examples.**

**A:**  
**Static Memory:**  
Allocated at compile time.  
Example:

```c
int a[10];
```

Limitations: fixed size, memory waste possible.

**Dynamic Memory:**  
Allocated at runtime using heap.  
Functions:

```c
malloc(), calloc(), realloc(), free()
```

Examples:

```c
int *p = malloc(10*sizeof(int));
p = realloc(p, 20*sizeof(int));
free(p);
```

**Comparison:**

- Static: size fixed, faster access
    
- Dynamic: flexible, but slower and requires management
    

Dynamic memory is ideal for unpredictable data sizes.

---

### **18. Q: Explain the complete lifecycle of a C program: from writing to execution.**

**A:**  
**1. Editing (Writing Code):**  
Using an editor (VS Code, Notepad++, etc.)

**2. Preprocessing:**  
Handles macros, header files, conditional compilation.  
Commands: `#include`, `#define`.

**3. Compilation:**  
Compiler converts code to machine code (.obj or .o).

**4. Linking:**  
Links object code with libraries to generate executable (.exe/a.out).

**5. Loading:**  
Loader loads program into memory.

**6. Execution:**  
CPU runs instructions starting from main().

Understanding lifecycle helps in debugging and optimization.

---

### **19. Q: Explain decision-making statements in C with examples (if, if-else, nested-if, else-if ladder, switch).**

**A:**  
Decision-making allows program to choose between options.

1. **if Statement:**
    

```c
if(a>0) printf("Positive");
```

2. **if-else:**
    

```c
if(a%2==0) print("Even"); else print("Odd");
```

3. **nested-if:**
    

```c
if(a>0) if(a>100) ...
```

4. **else-if Ladder:**
    

```c
if(x==1) ...
else if(x==2) ...
```

5. **switch-case:**  
    Used when multiple discrete values must be checked.
    

```c
switch(choice){ case 1:...; break; }
```

These constructs control the flow of program logically.

---

### **20. Q: Discuss the importance of modular programming. Explain how C supports modularity using functions and header files.**

**A:**  
Modular programming divides program into independent units (modules).

**Benefits:**

- Easier debugging
    
- Code reuse
    
- Better organization
    
- Simplifies teamwork
    
- Reduces complexity
    

**How C Supports Modularity:**

1. **Functions:**  
    Encapsulate logic.
    

```c
int sum(int a,int b){ return a+b; }
```

2. **Header Files:**  
    Contain prototypes and declarations shared across modules.
    

```c
#include "mathutils.h"
```

3. **Separate Compilation:**  
    Modules compiled separately and linked later.
    

Modular approach creates efficient, expandable, maintainable programs.

---

### **21. Q: Explain in detail the different types of storage classes in C. Discuss scope, lifetime, default value, and use-cases.**

**A:**  
Storage classes define the scope, lifetime, visibility, and default values of variables in C. The four types are:

---

### **1. auto**

- **Scope:** Local to the block
    
- **Lifetime:** Till function/block execution
    
- **Default Value:** Garbage
    
- **Use-case:** Normal local variables  
    Example:
    

```c
auto int x = 10;
```

---

### **2. static**

- **Scope:** Local to the block
    
- **Lifetime:** Entire program duration
    
- **Default Value:** Zero
    
- **Use-case:** Retain value across function calls  
    Example:
    

```c
static int count = 0;
```

---

### **3. extern**

- **Scope:** Global across multiple files
    
- **Lifetime:** Entire program
    
- **Default Value:** Zero
    
- **Use-case:** Sharing variables between files  
    Example:
    

```c
extern int x;
```

---

### **4. register**

- **Scope:** Local to the block
    
- **Lifetime:** Till block execution
    
- **Default Value:** Garbage
    
- **Use-case:** High-speed access  
    Example:
    

```c
register int i;
```

---

**Summary Table:**

|Class|Scope|Lifetime|Default Value|Use-case|
|---|---|---|---|---|
|auto|local|block|garbage|general variables|
|static|local|entire program|0|counters, caching|
|extern|global|entire program|0|cross-file access|
|register|local|block|garbage|high-speed variables|

Storage classes improve performance, modularity, and memory control.

---

### **22. Q: Explain functions in C in depth. Describe components, categories, arguments, recursion, return values, and advantages.**

**A:**  
Functions divide a large program into manageable parts.

---

### **1. Components of a Function:**

- **Function Prototype:** Declares function
    
- **Function Definition:** Contains logic
    
- **Function Call:** Invokes function
    

Example:

```c
int sum(int,int);    // prototype  
int sum(int a,int b){ return a+b; }  // definition  
sum(10,20);          // call
```

---

### **2. Categories:**

1. No argument, no return
    
2. No argument, with return
    
3. With argument, no return
    
4. With argument, with return
    

---

### **3. Argument Passing:**

- **Call by value:** Copies value
    
- **Call by reference:** Uses address of variable
    

---

### **4. Recursion:**

Function calling itself, used for factorial, Fibonacci, tree algorithms.

---

### **5. Return Values:**

Function may return single value using `return`.

---

### **6. Advantages:**

- Code reuse
    
- Clear structure
    
- Faster development
    
- Easy testing/debugging
    
- Modularity
    

Functions are central to structured programming.

---

### **23. Q: Describe pointer concepts in C with examples: declaration, arithmetic, arrays, functions, and dynamic memory.**

**A:**  
Pointer stores memory address and is one of C’s most powerful features.

---

### **1. Declaration and Initialization:**

```c
int x = 10;
int *p = &x;
```

---

### **2. Dereferencing:**

Access value stored at address.

```c
printf("%d", *p);
```

---

### **3. Pointer Arithmetic:**

Moves in multiples of data type size.  
Example:  
`p++` moves 4 bytes for int.

---

### **4. Pointers and Arrays:**

```c
int a[5]={1,2,3,4,5};
int *p = a;
```

`p[i] == *(p+i)`

---

### **5. Pointer to Function:**

```c
int (*fp)(int,int) = sum;
```

Allows callbacks.

---

### **6. Dynamic Memory:**

Using heap:

```c
p = malloc(5*sizeof(int));
free(p);
```

---

Pointers enable dynamic structures like linked lists, trees, and graphs. They provide performance and flexibility unmatched by many languages.

---

### **24. Q: Explain array and string differences. Discuss representation, limitations, operations, and memory behavior.**

**A:**

|Feature|Array|String|
|---|---|---|
|Type|Any type|Character array|
|Terminator|None|Ends with '\0'|
|Operations|Numeric|Text|

---

### **1. Array Representation:**

```c
int a[5]={1,2,3,4,5};
```

Stored in contiguous memory.

---

### **2. String Representation:**

```c
char s[]="HELLO";
```

Stored with null terminator.

---

### **3. Limitations:**

- Array: fixed size
    
- String: may overflow if null missing
    

---

### **4. Operations:**

Arrays → Sorting, searching  
Strings → Compare, copy, length

---

### **5. Memory Behavior:**

Arrays: 4 bytes per int (on most systems)  
Strings: 1 byte per char + '\0'

---

### **6. Example:**

String functions from `<string.h>`:  
`strlen`, `strcpy`, `strcmp`, `strcat`

Understanding differences helps choose proper data structure.

---

### **25. Q: Explain file handling functions in depth: fopen, fclose, fgetc, fgets, fprintf, fscanf, feof with examples.**

**A:**  
File handling enables storing data permanently.

---

### **1. fopen()**

Opens file.

```c
fp = fopen("data.txt","r");
```

---

### **2. fclose()**

Closes file.

```c
fclose(fp);
```

---

### **3. fgetc()**

Reads one character.

```c
char c = fgetc(fp);
```

---

### **4. fgets()**

Reads string including spaces.

```c
fgets(str,20,fp);
```

---

### **5. fprintf()**

Writes formatted output.

```c
fprintf(fp,"%d %s",id,name);
```

---

### **6. fscanf()**

Reads formatted data.

```c
fscanf(fp,"%d",&x);
```

---

### **7. feof()**

Checks end-of-file.

```c
while(!feof(fp)) { ... }
```

---

These functions handle structured I/O, essential for real-world applications.

---

### **26. Q: Explain in detail the concept of dynamic memory allocation, including malloc, calloc, realloc, and free. Discuss use-cases and memory management.**

**A:**  
Dynamic memory is taken from the heap at runtime.

---

### **malloc():**

Allocates uninitialized memory.

```c
int *p = malloc(5*sizeof(int));
```

---

### **calloc():**

Allocates zero-initialized memory.

```c
int *p = calloc(5,sizeof(int));
```

---

### **realloc():**

Resizes memory block.

```c
p = realloc(p,10*sizeof(int));
```

---

### **free():**

Frees allocated memory.

```c
free(p);
```

---

**Use-Cases:**

- When data size unknown
    
- Dynamic arrays, linked lists
    
- Efficient memory usage
    
- Runtime memory reallocation
    

Proper memory management prevents leaks and fragmentation.

---

### **27. Q: Explain decision-making statements in C with flowcharts and examples (if, nested if, else-if ladder, switch).**

**A:**  
Decision-making allows selection of one block among many.

---

### **1. if Statement:**

Executes when condition true.

```c
if(a>0) printf("Positive");
```

---

### **2. nested if:**

Multiple levels of checking.

```c
if(a>0)
   if(a%2==0)
```

---

### **3. else-if Ladder:**

Multiple conditions.

```c
if(x==1)...
else if(x==2)...
```

---

### **4. switch-case:**

Better for fixed values.

```c
switch(ch){
  case 1:...
}
```

Flowcharts visually explain flow of control between branches.

---

### **28. Q: Explain the complete compilation process in C: preprocessing, compiling, assembling, linking, loading, and execution.**

**A:**  
A C program passes through several steps:

---

### **1. Preprocessing:**

Handles macros, includes, conditional compilation.  
Output: expanded code.

---

### **2. Compilation:**

Converts code to assembly.

---

### **3. Assembling:**

Assembly → machine code (.obj/.o)

---

### **4. Linking:**

Links object code + libraries  
Produces executable (.exe/a.out)

---

### **5. Loading:**

Loader loads program into memory.

---

### **6. Execution:**

CPU executes instructions starting at main().

Understanding each step helps debug linker errors, compiler errors, and runtime failures.

---

### **29. Q: Explain in-depth the difference between structure and union. Discuss memory diagrams, size differences, and use-cases.**

**A:**

---

### **Structure:**

Each member gets separate memory.

```c
struct stu { int id; float marks; };
```

If int=4 bytes, float=4 bytes → struct size = 8 bytes.

---

### **Union:**

All members share same memory.

```c
union test { int x; float y; };
```

Union size = max(4,4) = 4 bytes.

---

### **Memory Diagram:**

Structure:

|id|marks|
|---|---|

Union:  
| shared space |

---

### **Use-Cases:**

Structure → student records, objects  
Union → memory-sensitive programs, embedded systems, variant fields

---

### **30. Q: Explain advantages and disadvantages of pointers. Discuss issues like dangling pointers, memory leaks, and wild pointers.**

**A:**

---

### **Advantages:**

1. Efficient array handling
    
2. Enables dynamic memory
    
3. Allows call-by-reference
    
4. Builds data structures
    
5. Faster access
    

---

### **Disadvantages:**

1. Complex syntax
    
2. Hard to debug
    
3. May cause crashes if misused
    
4. Pointer arithmetic risky
    

---

### **Common Pointer Problems:**

- **Dangling Pointer:**  
    Accessing memory after free
    
- **Memory Leak:**  
    Allocated memory not freed
    
- **Wild Pointer:**  
    Uninitialized pointer
    
- **Null Pointer Dereference:**  
    Using `NULL` pointer
    

Proper pointer handling is essential for secure C programming.

---